<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="ShelterVault.Views.CredentialsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ShelterVault.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:model="using:ShelterVault.Models" 
    xmlns:viewmodels="using:ShelterVault.ViewModels"
    mc:Ignorable="d">
    <UserControl.DataContext>
        <viewmodels:CredentialsViewModel x:Name="ViewModel"/>
    </UserControl.DataContext>
    <UserControl.Resources>
        <DataTemplate x:Key="CredentialListViewTemplate" x:DataType="model:Credential">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <FontIcon x:Name="Icon"
                    Width ="32"
                    Height="32"
                    Margin="6"
                    VerticalAlignment="Center"
                    HorizontalAlignment="Center"
                    Glyph="&#xE72E;"/>
                <TextBlock Grid.Column="1" VerticalAlignment="Center"
                    Text="{x:Bind Title, Mode=TwoWay}"
                    Style="{ThemeResource BaseTextBlockStyle}"/>
            </Grid>
        </DataTemplate>
    </UserControl.Resources>
    <Grid Margin="10" HorizontalAlignment="Center" VerticalAlignment="Top" MinWidth="600">
        <Grid.RowDefinitions>
            <RowDefinition Height="75"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <StackPanel>
            <Button>Add credentials</Button>
        </StackPanel>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="5*"/>
                <ColumnDefinition Width="5*"/>
            </Grid.ColumnDefinitions>
            <ListView x:Name="CredentialList" SelectionMode="Single" ItemTemplate="{StaticResource CredentialListViewTemplate}" ItemsSource="{x:Bind ViewModel.Credentials}" SelectedItem="{x:Bind ViewModel.SelectedCredential, Mode=TwoWay}"/>
            <StackPanel Grid.Column="1" HorizontalAlignment="Left" VerticalAlignment="Top" Width="300">
                <TextBlock Margin="0 0 0 5">Title:</TextBlock>
                <TextBox x:Name="SelectedTitle" Margin="0 0 0 10" Text="{x:Bind ViewModel.SelectedCredential.Title, Mode=TwoWay}"/>
                <TextBlock Margin="0 0 0 5">Username:</TextBlock>
                <TextBox x:Name="SelectedUsername" Margin="0 0 0 10" Text="{Binding SelectedCredential.Username, Mode=TwoWay}"/>
                <TextBlock Margin="0 0 0 5">Encrypted password:</TextBlock>
                <TextBox x:Name="SelectedEncriptedPassword" Margin="0 0 0 10" Text="{Binding SelectedCredential.EncryptedPassword, Mode=TwoWay}" IsReadOnly="True"/>
                <TextBlock Margin="0 0 0 5">URL:</TextBlock>
                <TextBox x:Name="SelectedUrl" Margin="0 0 0 10" Text="{Binding SelectedCredential.Url, Mode=TwoWay}"/>
                <TextBlock Margin="0 0 0 5">Notes:</TextBlock>
                <TextBox x:Name="SelectedNotes" Height="150" Margin="0 0 0 10" AcceptsReturn="True" TextWrapping="Wrap" Text="{Binding SelectedCredential.Notes, Mode=TwoWay}"/>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                    </Grid.ColumnDefinitions>
                    <Button Margin="0 0 0 10" HorizontalAlignment="Left" Command="{Binding DeleteCredentialCommand}">
                        <FontIcon Glyph="&#xE74D;" Foreground="Red"/>
                    </Button>
                    <Button Grid.Column="2" HorizontalAlignment="Right" Margin="0 0 0 10">
                        <FontIcon Glyph="&#xE8C8;" Foreground="{ThemeResource AccentAAFillColorTertiaryBrush}"/>
                    </Button>
                    <Button Grid.Column="4" HorizontalAlignment="Center" Margin="0 0 0 10">
                        <FontIcon Glyph="&#xF78D;" Foreground="{ThemeResource AccentAAFillColorDefaultBrush}"/>
                    </Button>
                    <Button Grid.Column="6" HorizontalAlignment="Center" Margin="0 0 0 10">
                        <FontIcon Glyph="&#xE74E;" Foreground="{ThemeResource AccentAAFillColorSecondaryBrush}"/>
                    </Button>
                </Grid>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
